package org.winside;

public class Application
{

    /**
     * Write a simple Algorithm than compresses a valid String with the following rules :
     *  - If a string contains spaces, it should delete them
     *  - If a character is repeated at least twice, the repeated character should be replaced with the character and its occurrence
     *  Example :
     *      "a" -> "a"
     *      "a b" -> "ab"
     *      "aabc" -> "a2bc"
     *      "follow the pressss" -> fol2wthepres4"
     * @param input
     * @return compressed String
     */
    public String compressString(String input) {

        return "";
    }
}
